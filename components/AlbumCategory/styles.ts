import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        margin: 10
    },

    title: {
        color: 'white',
        fontWeight: 'bold',
        fontSize: 30,
        margin: 10,
    }
});

export default styles;